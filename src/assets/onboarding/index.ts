import type { ImageSourcePropType } from 'react-native';

export const onboardingArt = {
  logbook: require('./logbook.png') as ImageSourcePropType,
  route: require('./route.png') as ImageSourcePropType,
  offline: require('./offline.png') as ImageSourcePropType,
};
